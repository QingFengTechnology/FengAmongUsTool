# 贡献指南

## 快速开始

### 在开始前

在为清风工具箱做出贡献前，请确保你具备以下条件：
- 你能使用源码启动清风工具箱。
- 你熟悉 Python 并了解 PyQt5 框架。
- 你熟悉 Qt Resource System (QRC) 并了解该如何使用。
- 你了解 Git 版本控制系统并知道如何使用 Git 的基本命令。
- 你应该了解 pip 包管理器并至少知道该如何使用 pip 安装项目依赖。

如果期望成为清风服工具箱核心开发成员，可加 QQ 群 1056158638 联系清风。

### 环境依赖

- [Python 3.13](https://www.python.org/downloads/)
- [Pip 25](https://pypi.org/project/pip/)
  - 本项目所需的依赖不在此处赘述，如果需要查看请阅读 requirements.txt。
- [Git](https://git-scm.com/downloads)

### 环境准备

克隆项目到本地：

> [!Note]
> 如果你想要向工具箱做出贡献，你应当 fork 此项目到你自己的 Github 账户再 clone 自己的仓库来进行开发。

```bash

git clone https://github.com/QingFengTechnology/FengAmongUsTool.git

# 或者，在你已经 fork 了该项目的情况下：
# git clone https://github.com/你的GitHub用户名/FengAmongUsTool.git

```

初始化虚拟环境(推荐)并安装项目依赖：

```bash

python -m venv venv

venv\Scripts\activate

# 如果你不希望通过虚拟环境运行，请忽略上述命令。

pip install -r requirements.txt

```

以源码启动清风工具箱：

```bash

venv\Scripts\activate

# 如果你已经进入虚拟环境(命令行前有(venv)标识)，或你不使用虚拟环境，请忽略上述命令。

python main.py

```

## 开发规范

### 代码规范

我们使用 ruff 来格式化和检查代码。请在提交代码前运行 ruff 来格式化代码。

```bash

ruff check

```

## 提交规范

> [!Note]
> 此规范参考自 [Angular 提交规范](https://github.com/angular/angular/blob/main/contributing-docs/commit-message-guidelines.md)。

每次提交信息包含**标题**、**正文**和**脚注**。

```text
<标题>
<空行>
<正文>
<空行>
<脚注>
```

标题是必需的，且必须符合[标题规范章节](#header)中规定的格式。\
大部分类型的提交需要正文。\
当存在正文时，其长度必须至少为20个字符，并且必须符合[正文规范章节](#body)中规定的格式。\
脚注是可选的。\
[脚注规范章节](#footer)格式说明了脚注的用途及其必须遵循的结构。

### <a id="header"></a> 标题

```
<类型>(<范围>): <摘要>
  │       │        │
  │       │        └─⫸ 对本次提交的总结，不应超过 50 个字符。
  │       │
  │       └─⫸ 可选，提交范围。用于指定本次修改影响了什么模块。
  │
  └─⫸ 提交类型: feat|fix|refactor|style|docs|test|build|ci|perf|chore|revert
```

`<类型>` 和 `<摘要>` 字段为必填项，`(<范围>)` 字段为可选项。

#### 类型

必须是以下选项之一：

| 类型         | 描述                                                                 |
| ------------ | -------------------------------------------------------------------- |
| **feat**     | 新增功能。                                                           |
| **fix**      | 错误修复。                                                           |
| **refactor** | 对代码进行重构，以提高代码质量或简化代码结构，但不更改功能行为。     |
| **style**    | 更改代码格式或样式，如修改缩进、空格、代码清理等，但不改变代码逻辑。 |
| **docs**     | 仅对文档相关的更改。                                                 |
| **test**     | 添加或修改测试用例或脚本。                                           |
| **build**    | 影响了构建系统或项目依赖。                                           |
| **ci**       | 对 CI 配置文件及脚本的更改。                                         |
| **perf**     | 优化代码，提高性能或减少占用。                                       |
| **chore**    | 杂项变更，如更新版本号、更新 .gitignore 等。                         |
| **revert**   | 撤销之前的提交。                                                     |

对于`docs`、`test`、`chore`类型的提交，正文是可选的。

#### 范围

通常应为受影响的模块或组件的名称，暂不对模块或组件名称作硬性要求。

#### 摘要

使用摘要字段提供简洁的变更描述：
- 不应超过 50 字符。
- 不以标点符号结尾。
- 不应添加修饰词，保持信息简洁直观。

### <a id="body"></a> 正文

在提交信息正文中解释更改的动机。\
此提交信息应解释你为何进行更改。\
你可以包含对先前行为与新行为的比较，以说明更改的影响。\
正文每行不应超过 72 个字符，如过长请正确断句并换行。

### <a id="footer"></a> 页脚

页脚可以是包含关于破坏性更改和弃用的信息，也是引用与此提交关闭或相关的 GitHub Issue 和其他 PR 的地方。例如：

```text
BREAKING CHANGE: <重大变更摘要>
<空行>
<重大变更说明 + 迁移指南>
<空行>
<空行>
Fixes #<Issue 编号>
```

破坏性变更部分应以短语 `BREAKING CHANGE: ` 开头，随后是破坏性变更的简要概述，一个空行，以及破坏性变更的详细描述，该描述还应包含迁移说明。

类似地，弃用部分应以 `DEPRECATED: ` 开头，随后是已弃用的内容的简短描述，一个空行，以及弃用的详细描述。

## PR 规范

与提交规范类似，PR 标题规范与[提交标题规范](#header)相同，正文规范与[提交正文规范](#body)相同。

对于有破坏性变更的 PR，我们建议你在正文前写一个 Github-Flavored Alerts 来提醒审查者注意：

```md

> [!Warning]
> 此 PR 包含 BREAKING CHANGE。

```

随后详细说明此破坏性变更，最终格式看起来像这样：

```text

> [!Warning]
> 此 PR 包含 BREAKING CHANGE。
<空行>
<破坏性变更说明>
<空行>
<正文>

```

## 命名规范

- 对于类名和 CI 配置文件，应使用大驼峰命名法。
- 对于方法和变量，应使用小驼峰命名法。
  - 私有方法应使用下划线(`_`)前缀。
- 对于文件或文件夹名，应使用蛇形命名法。
- 对于资源文件，应使用描述性名称，文件名只应包含字母且全小写。
  - 对于 i18n 文件，应使用`app.<语言代码>`格式，例如`app.zh_CN`。
  - 对于样式文件，与使用其该样式的文件名相同。